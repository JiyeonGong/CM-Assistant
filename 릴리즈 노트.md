# 릴리즈 노트

## 미배포

- 붙여넣기 출석부에서 외출 상태는 감지되지만 외출 시작/복귀 시간이 보고서에 표시되지 않던 문제를 수정했습니다.
  - `시작 시간`, `복귀 시간` 단독 헤더도 외출 시간 열로 인식합니다.
  - 보고서에는 `김코드(외출 13:00~14:00)`처럼 외출 시간까지 표시됩니다.
- 붙여넣기 표가 헤더 1줄과 데이터 1줄만 있는 짧은 표일 때도 분석할 수 있도록 최소 행 제한을 완화했습니다.
- 내 루틴 관리에서 `수정`이 동작하지 않고 `삭제`할 수 없던 문제를 수정했습니다.
  - 루틴 제목을 인라인으로 수정하고 저장/취소할 수 있습니다.
  - 루틴 삭제 버튼을 추가했습니다.
- 처리할 업무와 완료한 업무가 많을 때 페이지 전체가 길어지지 않도록 각 영역 내부에서 스크롤되도록 개선했습니다.
- 업무 탭의 카테고리 이동 메뉴를 Step 1 `업무 선택` 패널 안으로 옮겨 시선 이동을 줄이고, 업무 목록이 패널 내부에서 스크롤되도록 개선했습니다.
- 업무 탭 카테고리 탭을 스크롤 이동 방식에서 필터 방식으로 변경해 화면 움직임 없이 선택한 카테고리의 업무만 볼 수 있도록 개선했습니다.
- 업무 탭 `로그 대조` 카테고리에 오늘 날짜 기준 `로그 대조 결과` 공유 문구 템플릿을 추가했습니다.
  - 과정은 `DA`, `FS`, `FESI`, `SB`, `PD`, `AI`, `IF` 중 선택하고 기수만 입력하면 `[과정_기수기] n/n(요일) 로그기록 대조 결과 공유 (@행정지원매니저)` 형식으로 자동 반영됩니다.
- 로그 대조 카테고리의 자동 생성 문구를 복사 전 직접 수정할 수 있도록 변경했습니다.
- 로그 대조 결과 안내에서 변경 후 출석이 조퇴 또는 100분의50미만 계열이면 `Zep 접속시각` 대신 `Zep 퇴실 시각`으로 표시되도록 수정했습니다.
- 내 루틴 관리의 추가/수정/사용 여부 변경/삭제 안내를 하단 문구 대신 자동으로 사라지는 toast 알림으로 변경했습니다.
- 캘린더 선택 날짜 상세에서 개인/휴강/공휴일/연차 항목의 컬러 박스 배경을 제거하고, 일반 업무처럼 표시하되 메타 텍스트 색상만 카테고리별로 다르게 보이도록 개선했습니다.
- 오늘 탭의 진행 중 섹션에서 진행 중인 업무를 모두 표시하고, 오늘 루틴/추가 업무/진행 중/오늘 완료 섹션은 항목이 많으면 내부 스크롤되도록 개선했습니다.
- 상단 앱 이름 옆에 제작자와 문의 이메일을 확인할 수 있는 `?` 안내 아이콘을 추가했습니다.
- 오늘 탭에서 루틴/추가 업무를 완료해도 `오늘 완료` 영역에 표시되지 않던 문제를 수정했습니다.
  - 기존 저장 데이터에 `completedAt`이 없는 완료 업무도 오늘 날짜 업무이면 오늘 완료로 표시합니다.
- 출결 표 붙여넣기 입력창에 `×` 버튼을 추가해 기존 붙여넣기 내용을 한 번에 비울 수 있도록 개선했습니다.
- 출결상태가 `지각&외출`, `외출&조퇴`처럼 복합 상태일 때 외출 내역이 누락될 수 있던 문제를 수정했습니다.
- 출결상태가 `결석`으로 표시되어도 외출 시작/복귀 시간이 있으면 중간/최종 보고의 외출 내역에 포함되도록 수정했습니다.
- 출결 표 붙여넣기에서 외출 시작 시간만 표시되고 복귀 시간이 빠질 수 있던 문제를 수정했습니다.
- 외출 시작 시간만 있고 복귀 시간이 없으면 `신하람 10:05~`처럼 표시되도록 조정했습니다.
- 출결 표 붙여넣기에서 출석입력요청 헤더가 깨지거나 상위 헤더가 빠진 경우에도 행 데이터의 `신청/승인 + 휴가/공가`를 보조 탐지해 오전 보고 휴공가에 포함하고 최종 보고 결석에서 제외되도록 수정했습니다.
- 오전 보고는 입실 시간 기준만 보도록 조정해 결석을 항상 `0명`으로 표시합니다.
- 오후 보고에서 결석 라인을 제거했습니다.
- 오후 보고의 추가 휴공가는 항상 `0명`으로 출력되도록 조정했습니다.
- 최종 보고에서 외출자는 감지되지만 외출 시간이 누락되던 문제를 수정했습니다.
  - `외출 시간` 단일 열의 `11:30~14:30` 형식과 `시작 시간`/`복귀 시간` 분리 열을 모두 지원합니다.
